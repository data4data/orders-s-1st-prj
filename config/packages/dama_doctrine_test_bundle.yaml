# Every test runs in a transaction that is rolled back afterwards, so the schema is built once per
# run instead of before every test (Foundry detects the bundle).
when@test:
    dama_doctrine_test:
        enable_static_connection: true
        enable_static_meta_data_cache: true
        enable_static_query_cache: true
