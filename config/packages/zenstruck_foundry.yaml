when@dev: &dev
    zenstruck_foundry:
        persistence:
            flush_once: true

# Tests build the schema from the entities (includes test-only entities like TenantNote).
# Demo data in dev: `composer demo:reset` (runs the real migrations, then loads the story with --append).
when@test: *dev
